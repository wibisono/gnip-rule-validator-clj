(ns gnip-rule-validator.core-spec 
  (:require-macros [speclj.core :refer [describe it should=]])
  (:require [speclj.core]
            [gnip-rule-validator.core]))

(describe "A ClojureScript test"
  (it "fails. Fix it!"
    (should= 0 1)))
